name: CI

on:
  pull_request:
    branches: [main, staging]
  push:
    branches: [main, staging]

# One in-flight run per branch; newer pushes cancel older ones.
concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  check:
    name: Lint, format, test & build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7.0.0
      - uses: actions/setup-node@v6.4.0
        with:
          node-version: 22
          cache: npm
      # Drop the Windows-generated lockfile before installing — it records only
      # win32 optional deps, so npm ci on this Linux runner skips rollup/Next.js
      # linux binaries and the build step fails.
      - run: rm -f package-lock.json && npm install --no-audit --no-fund
      - run: npm run lint
      - run: npm run format:check
      - run: npm run test:unit
      - name: Build
        run: npm run build
        env:
          NEXT_TELEMETRY_DISABLED: '1'
          # Provide valid placeholders so the env schema doesn't warn.
          NEXT_PUBLIC_LIVE_URL: https://thrivo.fit
          NEXT_PUBLIC_API_URL: http://localhost:4000

  deploy:
    name: Deploy (Coolify)
    needs: check
    # Temporarily disabled: Cloudflare challenges GitHub Actions before the
    # request reaches Coolify, and the required WAF skip rule is a paid feature.
    if: ${{ false }}
    runs-on: ubuntu-latest
    environment: production
    steps:
      - name: Trigger Coolify deploy
        env:
          COOLIFY_BASE_URL: ${{ secrets.COOLIFY_BASE_URL }}
          COOLIFY_TOKEN: ${{ secrets.COOLIFY_TOKEN }}
          PUBLIC_UUID: ${{ secrets.COOLIFY_PUBLIC_APP_UUID }}
          CF_ACCESS_CLIENT_ID: ${{ secrets.COOLIFY_CF_ACCESS_CLIENT_ID }}
          CF_ACCESS_CLIENT_SECRET: ${{ secrets.COOLIFY_CF_ACCESS_CLIENT_SECRET }}
        run: |
          base_url="${COOLIFY_BASE_URL%/}"
          user_agent="thrivo-public-ci-deploy/1.0"

          if [ -z "$base_url" ] || [ -z "$COOLIFY_TOKEN" ] || [ -z "$PUBLIC_UUID" ]; then
            echo "::error::Missing one or more Coolify deploy secrets."
            exit 1
          fi

          curl --fail-with-body --show-error --silent --location --max-redirs 3 \
            --proto '=https' --tlsv1.2 \
            --user-agent "$user_agent" \
            -X GET "$base_url/api/v1/deploy?uuid=$PUBLIC_UUID" \
            -H "Accept: application/json" \
            -H "Authorization: Bearer $COOLIFY_TOKEN" \
            -H "CF-Access-Client-Id: $CF_ACCESS_CLIENT_ID" \
            -H "CF-Access-Client-Secret: $CF_ACCESS_CLIENT_SECRET"
